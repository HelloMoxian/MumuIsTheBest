# MumuIsTheBest 工程协作规则

## 1. 项目目标

这是一个面向约 5 岁儿童的技术启蒙与算术练习网站。产品优先级依次是：

1. 儿童可以独立理解和操作；
2. 大字、清晰反馈、低认知负担和足够大的触控区域；
3. 家长可以安全地查看、备份和恢复学习数据；
4. 架构保持简单、可本地长期运行、可逐步扩展。

## 2. 当前阶段

- 当前处于“正式应用初始化 + 数学与化学玩法持续实现”阶段。
- `temp/` 中的内容只用于比较视觉风格，不是正式产品代码。
- 正式 Node.js 技术栈已选定方案 A：React + Vite + Fastify；选型记录见 `docs/TECH_STACK_OPTIONS.md`。
- 正式视觉已选定为“星际探索舱”，基线原型为 `temp/style-02-galaxy.html`。
- 首个正式数学玩法“加减练习”已实现；题目边界、语音答案协议、正确率和历史数据语义见 `docs/ADD_SUBTRACT_PRACTICE.md`，后续修改必须保持兼容或同步升级该规范。
- 第二个正式数学玩法“算数大战”已实现；并行多题、唯一答案、难度、分段 ASR、间隔计时和完整结算规则见 `docs/ARITHMETIC_BATTLE.md`，后续修改必须保持兼容或同步升级该规范。
- 第三个正式数学玩法“乘法小能手”已实现；0—10 乘法、逆向整除、进阶乘除、唯一答案和历史规则见 `docs/MULTIPLICATION_PRACTICE.md`，后续修改必须保持兼容或同步升级该规范。
- 第四个正式数学玩法“神秘函数”已实现；九类函数模板、最多四条曲线、参数范围、定义域安全、坐标探针和实时粒子绘图规则见 `docs/MYSTERY_FUNCTION.md`，后续修改必须保持兼容或同步升级该规范。
- 首个正式化学模块“元素周期表”已实现；1—118 数据来源、标准布局、语音空间导航、动态原子模型与详情资料规则见 `docs/PERIODIC_TABLE.md`，后续修改必须保持兼容或同步升级该规范。
- 第二个正式化学模块“反应熔炉”已实现；物质资料库、分子式解析、随机 20 种目标、原子投放与实时聚合渲染规则见 `docs/REACTION_FURNACE.md`，后续修改必须保持兼容或同步升级该规范。
- 第三个正式化学模块“物质守恒”已实现；165 条常见反应、最简配平校验、三个难度、元素天平、单步提示与实时重排动画规则见 `docs/CONSERVATION_GAME.md`，后续修改必须保持兼容或同步升级该规范。
- `docs/DESIGN_SYSTEM.md` 是正式美术样式、组件状态和儿童交互原则的唯一规范；任何前端设计或实现前必须先阅读并遵循它。
- `apps/web` 和 `apps/server` 是正式产品代码；不要把 `temp/` 样稿迁入产品目录或作为运行时依赖。

## 3. 技术与依赖原则

- 使用 Node.js LTS、TypeScript strict mode 和单一包管理器；具体框架以用户最终选择为准。
- 前后端之间使用显式、可校验的契约；API 输入、文件数据和导入包都必须做 schema 校验。
- 优先选择成熟、维护活跃且能在 macOS、Windows 和 Linux 本地运行的依赖。
- 不引入数据库服务、ORM 或要求联网的运行时依赖。
- 不在浏览器端直接操作服务端文件。
- 不为了“以后可能用到”而提前增加服务、队列、容器或微服务。

## 3.1 阿里云实时 ASR（强制）

- 实时识别使用阿里云百炼 Fun-ASR 的 WebSocket API；协议细节与安全架构见 `docs/ASR_REALTIME.md`。
- API Key 是秘密信息：禁止写入 Git、代码、文档、日志、截图、测试 fixture 或浏览器存储。唯一允许的持久化位置是本机 Git 忽略的数据文件 `var/config/asr-settings.json`（或 `APP_DATA_DIR/config/asr-settings.json`），必须原子写入且限制为当前用户可读写。
- 浏览器不得直接连阿里云 ASR。浏览器音频必须通过同源 Fastify WebSocket 入口转发，由服务端在上游 WebSocket 握手中附加 Authorization。
- 页面是本机可视化配置入口：页面重新打开时只能获知“已配置”和端点，绝不回传或显示 Key 明文；粘贴新 Key 并保存后，前端必须清空该字段，服务端不得回显或打印它。
- ASR 转接服务只能连接经校验的阿里云百炼 `wss://*.maas.aliyuncs.com/api-ws/v1/inference` 地址，不能成为通用 WebSocket 代理。
- 所有上游失败必须转换为不含密钥的用户可读错误；不得把请求头、原始鉴权异常或完整上游数据直接暴露到页面。
- 每个 ASR 实时会话默认且强制限制为最多 2 分钟（120 秒）。服务端必须从上游 `task-started` 起计时并在到时发送 `finish-task`；前端倒计时只是辅助，不能替代服务端限制。除非用户明确变更本规则，后续所有游戏、算术和知识模块接入 ASR 时一律继承它。

## 4. 代码与数据隔离（强制）

仓库中的内容分成三类，禁止混用：

### A. 可版本控制的产品内容

- 源代码、测试、文档、静态资源。
- 开发者有意维护的默认题库、课程模板和示例数据，放在 `content/`。
- `content/` 中不得包含孩子姓名、学习记录、家庭信息或运行时生成的数据。

### B. 运行时个人数据

- 孩子的答题历史、进度、星星/徽章、家长在页面中录入的知识点、偏好设置等。
- 默认写入仓库根目录下的 `var/`；允许使用环境变量 `APP_DATA_DIR` 指向仓库外的绝对路径。
- `var/` 必须被 Git 忽略。应用不得提示用户提交、暂存或手工编辑这些文件。
- 正式实现时，启动日志必须显示最终使用的数据目录，但不得打印个人数据内容。
- 正式实现时，文件写入必须使用临时文件 + 原子替换；同一数据文件必须有单写入队列或文件锁，避免并发损坏。
- 每个持久化文件必须包含 `schemaVersion`、稳定 ID、创建时间和更新时间；读取时必须校验 schema，迁移必须可测试、可回滚。
- 不允许把运行时写入放到 `src/`、`app/`、`public/`、`content/` 或任何 Git 跟踪目录。

### C. 备份与导入导出

- 默认备份目录为 `backups/`，也允许用户指定仓库外目录；该目录必须被 Git 忽略。
- 备份功能在后续模块中实现。目标格式应是带 manifest、schema 版本和校验信息的可移植压缩包。
- 导出应生成新文件，不覆盖唯一一份现有备份；导入前必须验证格式，并先为当前数据创建恢复点。
- 自动备份采用保留策略（例如日/周/月）时，删除旧备份前必须确认至少存在一份可验证的新备份。
- 任何包含个人数据的备份都不得自动上传、提交 Git 或写入公共目录。
- 测试只能使用脱敏 fixture，放在测试目录；不得复制真实运行时数据。

## 5. 儿童体验与可访问性

- 正文默认字号不小于 18px；主要题目与关键数字应明显更大。
- 主要点击/触控目标最小 48 × 48px，目标之间保留足够间距。
- 不只依赖颜色表达正确、错误或选中状态；同时使用文字、图形或形状变化。
- 动效应短、友好、有目的；必须支持 `prefers-reduced-motion`。
- 禁止突然自动播放声音。音效需要明确开关并尊重静音状态。
- 错误反馈不得羞辱、催促或制造焦虑；使用鼓励式措辞，并允许再次尝试。
- 儿童操作区与家长设置/数据管理区应在信息层级和入口上明确区分。

## 6. 视觉一致性

- `docs/DESIGN_SYSTEM.md` 是界面视觉的唯一事实来源，且已正式生效。若与临时样稿或任何旧页面冲突，以该文档为准。
- 视觉基线固定为“星际探索舱”：深空背景、半透明深蓝玻璃面板、紫/青/粉任务光效、圆润大字号组件和克制的探索动效。
- 新页面必须复用其中的颜色 token、字体层级、圆角、阴影、间距、动效和组件状态；不得在页面中散落硬编码的替代视觉规则。
- 新增 UI 不能只实现“可用”的默认态。必须按设计系统处理默认、hover、pressed、focus-visible、disabled、loading、success、error、empty 和移动端状态；不适用时要说明原因。
- 新的通用 UI 模式必须先扩展 `docs/DESIGN_SYSTEM.md`，再在正式组件层实现和复用；不得在单个业务页面临时发明一套视觉语言。
- 不得引入纸艺、街机、糖果、拟物等第二主视觉，也不要用自动声音、频繁闪烁或高压反馈换取“活泼感”。
- emoji 只能作为辅助插图，不能代替关键操作的文字标签。
- `temp/` 只保留为选择记录；正式开发不得运行时依赖它。

## 7. 质量门槛

- 每个业务模块至少覆盖：成功路径、非法文件/输入、空数据、版本迁移和写入失败。
- 文件存储相关测试必须使用临时目录，不得触碰真实 `var/` 或 `backups/`。
- 交互组件需要键盘可用、焦点可见，并提供可理解的 label。
- 合并前运行所选技术栈规定的格式化、类型检查、单元测试和生产构建。
- 涉及数据格式的变更，必须同步 schema、迁移、fixture、导入导出版本和文档。

## 8. Git 规则

- 不提交运行时数据、备份、日志、密钥、构建产物或依赖目录。
- `.env.example` 只写键名和安全示例；真实 `.env*` 不提交。
- 不将个人信息写入 issue、提交信息、测试快照或截图。
- 对数据目录和备份规则的修改属于高风险变更，需要在交付说明中明确列出。
