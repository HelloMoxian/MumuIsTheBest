# 木木学习岛：项目状态总览

状态：**首页、语音底座与正式学习模块已完成并持续扩展**
更新时间：2026-08-25

本文是当前已确定规则、已实现能力和后续边界的总览入口。详细的技术、安全和视觉规则以链接的专项文档为准。

## 产品方向

- 面向约 5 岁儿童的技术启蒙与算术学习网站。
- 核心价值是“木木最努力”：鼓励好奇、尝试、坚持和一点点进步，而非强调天生聪明或排名竞争。
- 体验定位是学习与娱乐结合的星际学习乐园；题目、反馈和下一步操作必须始终比装饰更醒目。

## 已确定的工程规则

| 领域 | 当前规则 |
|---|---|
| 技术栈 | Node.js 24+、TypeScript strict、pnpm workspace、React + Vite 前端、Fastify 后端。|
| 数据 | 不使用数据库；默认题库等可版本控制内容与孩子的运行时数据严格分离。|
| 运行时数据 | 默认写入仓库同级 `../data/`；可通过绝对路径 `APP_DATA_DIR` 指向其他仓库外位置。|
| 备份 | 默认目录为 `../data/backups/`；导入导出、校验与恢复将在具体数据模块落地时实现。|
| 美术 | 唯一视觉基线为“星际探索舱”：深空玻璃面板、紫/青/粉任务光效、圆润大字号和克制动效。|
| 儿童体验 | 正文大字、48px+ 触控目标、温和可恢复的反馈、无障碍焦点、支持减弱动效；不以时间或失败制造压力。|
| 文字朗读 | 使用浏览器原生 Web Speech API 的统一前端服务；无需 Key 或付费接口，优先本地声音，长内容自动分句。|
| 启动 | 根目录 `./init_and_start.sh` 负责检查环境、安装依赖、清理本项目端口、启动前后端并打开网页。|

完整工程规则见 [`AGENTS.md`](../AGENTS.md)，数据清单与迁移见 [`DATA_STORAGE.md`](./DATA_STORAGE.md)，技术选型见 [`TECH_STACK_OPTIONS.md`](./TECH_STACK_OPTIONS.md)，视觉规范见 [`DESIGN_SYSTEM.md`](./DESIGN_SYSTEM.md)。

## ASR 安全与使用规则

- 实时识别使用阿里云百炼 Fun-ASR；浏览器仅连接同源 Fastify WebSocket，后端在上游握手时添加鉴权头。
- API Key 永不写入 Git、代码、日志、截图、测试或浏览器存储。
- 语音测试页是本机配置入口。Key 仅保存于仓库外 `../data/config/asr-settings.json`（或 `APP_DATA_DIR`，文件权限仅当前用户可读写）；页面只显示已配置状态，绝不回显明文。
- 单次实时 ASR 会话从上游 `task-started` 起**强制最多 10 分钟**。服务端负责到时发送 `finish-task`，前端倒计时仅作辅助。未来任何算术、游戏或知识模块接入 ASR 时默认继承此规则。
- 不保存孩子的音频或识别文本；所有错误提示不得泄露 Key、请求头或上游原始鉴权异常。

协议、安全架构和手动测试方式见 [`ASR_REALTIME.md`](./ASR_REALTIME.md)。

## 已实现能力

### 全局知识币与三倍玩法

- 主页与加减练习、算数大战、乘法小能手、找数字、猫鼠游戏顶部共用服务端知识币余额。
- 系统每十分钟稳定随机其中一个玩法获得三倍奖励；进入页面时把倍率写入持久化奖励场次，后续轮换不改变本局。
- 基础奖励为：加减练习每题 2 币；算数大战简单 / 中等 / 超难每题 4 / 6 / 8 币；乘法小能手乘法 / 逆向除法 / 进阶乘除每题 2 / 3 / 5 币；找数字四档每局 10 / 30 / 60 / 150 币；猫鼠游戏每题 20 币。三倍场次在对应基础分上乘三。
- 服务端确认入账后逐枚金币飞向顶部；大额奖励显示完整入账数，减弱动效下关闭位移。

### 通用浏览器文字朗读

- 前端已提供统一的 TTS 服务和 React Hook，后续题目、阅读材料、知识卡片与提示语共享同一套调用、声音选择和播放状态。
- 服务直接使用浏览器 Web Speech API，不依赖 API Key、付费云服务或新增第三方包；默认使用 `zh-CN` 并优先设备本地声音。
- 长文本按句子和自然标点自动分段，支持顺序朗读、暂停、继续、停止、句子回调和与长度相关的安全超时。
- 新内容会取消旧内容；切题或离页后的旧朗读返回 `cancelled`，不会继续打开旧题的 ASR。
- 浏览器不支持、语言包缺失或网络声音失败时只降级声音，始终保留可见内容与核心学习流程。
- 加减练习已经迁移到该服务，并保持“先朗读、再打开 ASR”的半双工顺序。

完整架构、用法、隐私边界与测试要求见 [`WEB_SPEECH_SYNTHESIS.md`](./WEB_SPEECH_SYNTHESIS.md)。

### 主页与设计系统

- 正式品牌为“木木学习岛”，首页主题为“木木最努力”。
- 内置 40 条努力学习鼓励语，每次进入页面随机展示一条。
- 首页包含“探索地图”：游戏、数学、英语、化学、语文、国学六个一级目录。
- 每行左侧为学科舱和 CSS 学科插画，右侧玩法区在桌面端使用等宽卡片呈现正式可用入口。
- 当前正式入口包括：

  - 数学：加减练习、算数大战、乘法小能手、神秘函数、找数字。
  - 游戏：切水果。
  - 英语：英语回声岛。
  - 化学：元素周期表、反应熔炉、分子工厂、实验大师、物质塔。
  - 语文：拼音星桥、常用 500/1000/1500/2000/2500 字。
  - 国学：声律启蒙。
- 顶部提供全局数字键盘：以不带遮罩、不锁滚动的右侧紧凑辅助抽屉展开，连续点击组成十进制整数，逐位显示个、十、百、千、万等位值和完整中文读法，并把显式提交交给当前数字玩法的原有判题流程；提交后保持展开以支持连续答题。

### 游戏：切水果

- 首页新增“游戏”一级分类和“切水果”正式入口。赛前可设置单/双人、六个家庭角色、30—120 秒时长、四档密度、速度、物体大小、滑动灵敏度、炸弹和龙虾。
- 浏览器本机使用 MediaPipe Pose Landmarker 与 Hand Landmarker 识别最多两个人、四只手；摄像头帧不上传、不保存，GPU 初始化失败自动回退 CPU，无摄像头时保留单人鼠标练习。
- 掌心轨迹经指数平滑并用二次贝塞尔连续绘制；只有同时达到位移和速度阈值才参与碰撞，普通切片得分与速度正相关。
- 普通水果按 2—6 个组成高低、疏密不规则的弧线、斜线或扇形波次，整体飞行约为旧版半速；默认物体尺寸 160px，并使用随尺寸同步扩大的碰撞核，便于一挥连续切中多个水果。
- 双人局左右同时飞出相同物体并使用镜像轨迹，双方独立计分、连击和超级模式。2.5 秒内六次命中触发 6 秒三倍得分；炸弹只会单独成波且固定扣 30 分，龙虾可连续切三次。
- 六个角色分别统计总局数、每局分数和最高分；双人统计每对角色的各自胜局与平局。完整记录原子写入仓库外文件。
- 能量币与知识币独立。只有木木达到 500 分后按每 500 分 1 枚、单局最多 20 枚由服务端幂等发放；当前可由“岩石与矿物”按固定 30 枚购买地质锤，收入和消费共用切水果钱包的幂等账本。

完整识别、计分、双人镜像、资产、历史与能量币规则见 [`FRUIT_SLICE_GAME.md`](./FRUIT_SLICE_GAME.md)。

### 化学：物质塔

- 已按用户确定的十六层扩展为 503 个精选节点；基本粒子、118 种元素、常见化学物质、生命、文明、工业、地球、太阳系、银河、星系和宇宙层全部在同一张连续图中呈现。
- 503 个节点已全部具备语义图片：保留原 15 张固定 5×4 图集和 296 个旧生成节点，并以两张固定 3×4 图集补充 24 个生活化化学节点；源图、稳定格位、透明切图和自动校验均已纳入工程。
- `content/world-tower/composition-rules.v2.json` 是唯一人工规则源。每个非起点节点只保留 1—3 个前置、关系类型和知识主题，不再维护动作、条件、环境、粒子包或知识背包。
- 主图不抽样、不分页、不需要逐层展开；每行根据宽度安排 6—11 个节点，118 种元素紧凑换行。常态隐藏关系线，选中后只显示直接来路与去向。
- 左侧十六层导航与中央长图独立，始终留在屏幕内；中央图滚动时自动高亮当前层级，点击层级可直接定位。
- 基本粒子每个 1 知识币，元素 5 币，逐层约增加 5 币，宇宙级 75 币。旧图谱迁移时保留知识币与奖励历史，清空不兼容的旧节点和背包进度。

### 实时语音识别测试

- 顶部“功能测试”菜单提供“语音识别测试”入口；菜单层级已独立处理，不会被首页面板遮挡。
- 页面可保存本机 ASR 配置、显示配置状态、接收麦克风 PCM 音频并在右侧展示中间/最终识别结果。
- 识别测试包含可理解的网络、Key、端点、麦克风权限和超时错误提示。

### 数学：加减练习

- 首页“数学 → 加减练习”已成为正式可用入口，页面支持 5/10/20 题、加/减/混合和不朗读/中文/英文配置。
- 题目严格限制为操作数和结果均在 0—20；加减混合局同时包含两种运算，同一局不重复算式。
- 每题使用独立 ASR 会话，只判定“等于 / =”后的阿拉伯或中文数字；支持 `十二`、`一十二`、`零十二` 等识别差异。
- 错误答案会显示“再想一想”并持续监听；本题一旦说错，最终答对后仍只按一次“非首次正确”统计。
- 全部完成后显示逐题结果并写入 Git 忽略的本机历史文件；中途关闭不保存。历史面板按题数和运算类型聚合耗时、年龄与正确率。
- 页面使用专属原创星际数学实验室插画、本地程序化成功音和完整的移动端/减弱动效样式。

完整规则和数据语义见 [`ADD_SUBTRACT_PRACTICE.md`](./ADD_SUBTRACT_PRACTICE.md)。

### 数学：算数大战

- 首页“数学 → 算数大战”已成为正式可用入口，支持同时出现 1—5 题和简单/中等/超难三个档位。
- 同一局算式和答案均保持唯一，孩子说出一个“等于 + 答案”时最多点亮一题，并可自由决定解题顺序。
- 简单题聚焦带进退位的两位数加减；中等题混合乘法与整除除法；超难题使用 2—4 个数和 300 以内的综合运算，并避免低复杂度随机题。
- 正确结果每 200ms 检查并立即点亮；未命中任何题目的识别结果等待 2 秒确认后显示温和提示，识别过程不中断。
- 每段 ASR 仍由服务端限制为 10 分钟，本玩法最多顺序续接 5 段；第 5 段结束仍未完成则不生成成绩。
- 逐题耗时按相邻正确答案的识别间隔计算。只有全部完成才以 100 分、100% 正确率结算，并写入独立的 Git 忽略历史文件。
- 历史面板按题目数量和难度聚合平均总耗时、单题耗时、计算时间、年龄与正确率。

完整规则和数据语义见 [`ARITHMETIC_BATTLE.md`](./ARITHMETIC_BATTLE.md)。

### 数学：乘法小能手

- 首页“数学 → 乘法小能手”已成为正式可用入口，复用纵向 1—5 题并行语音挑战、分段 ASR、间隔计时和完整结算模式。
- “0—10 乘法”覆盖两个乘数 0—10，并允许 `7 × 8`、`8 × 7` 等先后顺序变化。
- “逆向除法”由 0—10 乘法事实反向生成，被除数不超过 100、除数为 1—10，保证整除且绝不除以 0。
- “进阶乘除”混合 20 以内乘法与 100 以内整除，乘法结果不超过 400；同一局题目答案始终互不相同。
- 只有全部题目解出才以 100 分结算，完整成绩写入独立的 Git 忽略历史文件；历史面板按题目数和难度聚合。

完整规则和数据语义见 [`MULTIPLICATION_PRACTICE.md`](./MULTIPLICATION_PRACTICE.md)。

### 数学：神秘函数

- 首页“数学 → 神秘函数”已成为正式可用入口，用开放式实验代替计分题目，让孩子直接观察公式、参数与图像之间的关系。
- 公式库包含一次、二次、三次、正弦、余弦、绝对值、平方根、反比例和指数九类常见函数；每个参数都有儿童可理解的名称、含义、范围和安全步进。
- 同一坐标系最多展示四条曲线。曲线使用固定颜色、编号、名称和实时公式共同辨认，可以选择、隐藏、显示、收起并加入同类函数的不同参数版本。
- Canvas 按设备像素比绘制坐标轴、数字网格和曲线；参数变化后播放约 820ms 的从左到右扫描光与粒子汇聚。平方根按定义域绘制，反比例在渐近线两侧分段，不连接不连续点。
- 坐标范围支持 `±6 / ±10 / ±20`。鼠标或触控拖动可放下 `x` 探针，页面以文字同时列出每条可见曲线对应的 `y`。
- 页面具备介绍、开始、重置、结束总结、继续探索、再开一次和返回大厅的完整流程；结果页总结观察过的函数家族、参数调整次数和最终公式，当前不写入个人数据。

完整模板、参数边界、绘图与交互规则见 [`MYSTERY_FUNCTION.md`](./MYSTERY_FUNCTION.md)。

### 数学：找数字

- 首页“数学 → 找数字”已成为正式可用入口，支持 `0—100 / 0—1,000 / 0—10,000 / 0—100,000` 四档包含两端点的整数范围。
- 语音可控制“开始一局 / 结束一局 / 下一局”，并解析“是 500 吗 / 小于 800 吗 / 大于 300 吗 / 不超过 / 至少”等比较问句；中文数字覆盖零至十万。
- 同一条全范围数轴在整局中保持固定。候选区使用青粉发光带，已排除区使用灰色星尘冻结带、斜纹和裂纹；每次新排除区播放短暂扫描和星屑消散。
- 所有 `< / > / ≤ / ≥` 都按闭区间精确更新，页面持续显示候选左右端点、剩余数量、累计排除数量、本局耗时和当前中间数提示。
- 每次有效提问都记录可见问句、关系回答、提问时刻、本次新排除数量、累计排除数量和剩余数量。重复或范围外提问保留记录但不会错误缩小范围。
- 精确命中后醒目揭晓目标并完整回放提问过程；主动结束只温和揭晓，不伪装为通关。两种结果都支持下一局、换范围和返回大厅。
- 页面复用阿里云 Fun-ASR 同源安全转接，每段仍强制最多 10 分钟，到时保留进度并可继续；语音不可用时保留关系选择、数字输入和提交按钮。当前玩法不写入个人数据。

完整语音协议、边界语义、数轴动画和验收规则见 [`FIND_NUMBER_GAME.md`](./FIND_NUMBER_GAME.md)。

### 化学：元素周期表

- 首页“化学 → 元素周期表”已成为正式可用入口，按标准 18 族、7 周期布局展示原子序数 1—118，并在底部独立展示完整镧系和锕系。
- 每个元素格包含原子序数、元素符号、中文名、带声调拼音和相对原子质量；10 类元素同时用颜色、图例文字和选中描边区分。
- 默认选中氢，支持鼠标/触控、方向键和语音“上 / 下 / 左 / 右”空间导航；连续方向词逐个消费，到达边缘不循环。
- 语音“详细信息 / 返回”打开或关闭元素档案；ASR 中间与最终结果按句子增量消费，避免全量结果让光标重复移动。每段识别继续继承服务端 10 分钟上限。
- 118 个元素都有用途概述。详情弹窗展示物理和原子资料、化学性质、真实电子层、发现年代、常见氧化态和用途，并从统一 518 条目录计算全部相关物质；清单支持搜索、12 条分页和丰富档案，502 条记录可展示本地 PubChem 二维结构图。
- 动态原子模型按每个元素的真实电子层和电子数渲染；原子核稳定在中央、内层电子灰蓝、最外层电子高亮并沿独立椭圆轨迹缓慢运动。桌面端左栏在详情滚动时保持可见。
- 基础数值来自 PubChem，中文名称参照中国化学会发布的 IUPAC 中文元素周期表；未知和预测性质保持明确标记。

完整数据来源、交互和详情语义见 [`PERIODIC_TABLE.md`](./PERIODIC_TABLE.md)。

### 化学：反应熔炉

- 首页“化学 → 反应熔炉”已成为正式可用入口；运行时资料库包含 518 种物质，由 297 种安全的原始分子、68 种元素覆盖化合物和 153 种教材常见物质/碳材料结构组成。
- 资料库共有 268 种有机物和 250 种无机物，覆盖原子序数 1—80 的全部元素；原子序数 21—80 的金属均至少有两种记录且至少一种不是二元氧化物。
- 每批先随机确定 10 种优先元素，再选择能覆盖它们的 8 种无机物和 2 种有机物；最终至少出现 10 种元素，避免从全池直接抽样造成重复。
- 右侧原子仓严格按本批 10 种物质的总需求生成。少量原子逐个投放，大量同类原子按最多 10 个一组投放，支持 C₆₀ 等大结构。
- 炉体使用实时 Canvas 绘制能量云、星尘、投放波纹、原子布朗运动和边界碰撞；配方齐全后依次播放靠近、震动、键位发光、局部闪光和稳定漂移。
- 当前随机批次、已投放/游离原子、已稳定结构与批次编号会安全保存在仓库外本机文件；重新打开后恢复原子清单和结构停泊位，动画中断时会把相关原子归还到游离清单再继续匹配。旧浏览器缓存会在服务端文件为空时一次性迁移。
- 解析器支持上下标、括号、方括号配合物、结晶水与前置系数，并过滤变量聚合物、未知元素符号和异常大配方。
- 离散分子使用 PubChem 或同行评议来源的真实连接拓扑并用实线显示；离子晶体、盐和水合物用虚线连接组成示意，虚线明确不代表共价键。金刚石、碳纳米管和石墨烯显示有限的代表性晶格片段，C₆₀ 显示完整 60 原子球形笼。
- 页面明确标注“虚拟组成演示，不是实验步骤”，不提供危险物质的可操作制备条件；减弱动效模式会关闭持续运动并缩短聚合动画。

完整资料库、组成语义和实时渲染规则见 [`REACTION_FURNACE.md`](./REACTION_FURNACE.md)。

### 化学：分子工厂

- 首页“化学 → 分子工厂”已成为正式可用入口，继续以每行 9 格的紧凑网格展示前 90 号元素，并按氢/碳 5 个、氧 3 个、其他元素 1 个投放。
- 顶部新增“不创造有机物”和“自动合成”两个可访问勾选项。自动模式保持 520 毫秒停顿后匹配；手动模式随机显示最多 3 张可组成物质卡，约 5 秒漂浮并淡出，点击才扣除原子。
- “不创造有机物”同时过滤自动匹配和手动候选，不删除历史收藏。候选继续排除单原子记录并按组成去重。
- 10 种常见原子团在配方满足时通过可见动效逐个形成，每类同时只显示一个标准式、中文名和电荷；形成后从游离原子池扣除，并作为完整中间库存优先参与化合物。需要多个同类原子团时使用一个完整原子团，其余由散装原子补足。
- 原子团和化合物共用串行构建队列，构建状态卡与克制闪光明确展示当前项目；清空操作同时移除游离原子和原子团并保留成品。金刚石在儿童界面中改用 1—2—3—4 四行三角锥式代表布局。
- 同一物质本次探索只生成一次。结构从反应区底部离场并进入成品收藏架；元素清单中的“× 移除”会解除完成状态。
- 游离原子、收藏、选中元素、两个选项、原子团和被中断聚合会保存在仓库外本机文件，并在文件为空时兼容迁移旧分子工厂/聚宝盆/聚宝盒浏览器缓存。
- 单原子单质不会生成收藏，多原子单质允许生成；页面继续强调这是虚拟组成演示，不提供现实制备步骤。
- 518 条统一运行时目录为分子工厂、反应熔炉和元素周期表的唯一化合物事实来源；502 条记录配有本地 PubChem 二维结构图。

完整元素范围、双模式、原子团、统一资产和离场规则见 [`MOLECULE_FACTORY.md`](./MOLECULE_FACTORY.md)。

### 语文：拼音星桥

- 首页“语文 → 拼音星桥”已升级为独立专项页面，不再复用 5/10 题短任务底座。
- 首页单屏展示 `23 个声母 / 24 个韵母 / 16 个整体认读音节` 共 63 个学习单元；桌面同时展示三类，窄屏使用不滚动的三段分区切换。
- 点击拼音格会从现有 2500 常用字资料中随机抽取最多 6 个相关汉字，展示大字、完整带调拼音和组词；当前声母、韵母或整体音节在完整拼音中保留声调并加亮。
- 详情舱桌面使用 3 列 × 2 行、窄屏使用 2 列 × 3 行，适配当前视口且不出现内部纵向滚动；支持“换一批汉字”继续探索。
- 鼠标/触控、键盘方向键和语音共享选中状态；语音覆盖方向导航、三个分区、详细信息、换批、关闭和返回首页，每段继续继承 10 分钟上限。
- 当前玩法不记录浏览历史或成绩，汉字和组词复用 `content/chinese/common-characters.v1.json`，不维护重复内容源。

完整范围、匹配、高亮、响应式和语音协议见 [`PINYIN_STAR_BRIDGE.md`](./PINYIN_STAR_BRIDGE.md)。

### 语文：常用汉字

- 首页“语文”已接入“常用 500 / 1000 / 1500 / 2000 / 2500 字”五个正式入口，全部复用同一套玩法，只改变频率前缀字池。
- `content/chinese/common-characters.v1.json` 包含 2500 个互不重复的一级规范汉字、9978 个组词和 4592 条成语记录；拼音、部首、笔画、简明释义和儿童朗读例句均随字提供。
- 内容生成同时参考规范字频率、HSK 分级词、SUBTLEX-CH 词频和新华字典候选，并通过独立脚本检查数量、前缀池、乱码、拼音、组词、例句和成语结构。
- 单局支持 5/10 字。汉字立即出现，约 1.7 秒后显示拼音，约 3.9 秒后显示释义、组词、例句和成语；桌面学习态固定在一个视口内。
- 语音覆盖“开始练习 / 我会了 / 下一个 / 详细信息 / 结束一局 / 再来一局 / 文字清单 / 返回”，增量和整句 ASR 按句子与语义去重，每段继续继承 10 分钟强制上限。
- 自适应抽题按“需要复习 / 新字 / 已掌握复习”混合选择；没说“我会了”的字只标记为继续复习，不使用答错或失败表述。
- 学习次数、我会了次数、继续复习次数和最近时间原子写入仓库外 `../data/learning/chinese/common-characters-progress.json`；文字清单按频率分页展示并支持搜索、状态筛选。

完整内容来源、交互、语音、自适应和数据语义见 [`COMMON_CHARACTERS.md`](./COMMON_CHARACTERS.md)。

### 化学：实验大师

- 首页“化学 → 实验大师”提供 5/10 个发现的观察任务，包含单选与顺序选择两类互动。
- 当前 10 个任务围绕溶解、过滤、蒸发、磁铁、材料与安全观察展开；每题都提供具体观察提示、知识结论和儿童可理解的解释。
- 朗读复用通用 Web Speech 服务，并与 ASR 严格按半双工顺序协作；语音不可用时全部核心操作仍可由大按钮完成。
- 首次未发现不会自动切题或写“答错”，而是给出具体观察方向；结算只区分“一次发现 / 观察后发现”。
- 当前玩法不写入儿童个人数据，中途结束只总结本次页面内已经完成的发现。

完整内容边界、交互和验收规则见 [`EXPERIMENT_MASTER.md`](./EXPERIMENT_MASTER.md)。

## 当前目录职责

```text
apps/web/       # 儿童端首页、学科地图、语音测试、数学玩法与化学互动模块
apps/server/    # Fastify API、ASR 安全转接、配置与各玩法历史读写
docs/           # 技术、设计、ASR 和项目状态文档
temp/           # 视觉选型样稿，仅供参考，不参与运行
content/        # 后续可提交的默认题库和课程模板
../data/        # 仓库同级的本机运行时数据、ASR 配置、日志与未来备份
```

## 后续实施顺序

1. 使用真实阿里云账号完成一次中文连续语音的人工验收，重点观察最终句边界和不同口音下的中文数字。
2. 为木木年龄增加家长设置入口，后续历史记录继续按每局实际年龄保存。
3. 实现家长侧的数据查看、显式备份、导入和恢复；所有文件操作遵循原子写入和 schema 校验规则。
4. 继续完善三个语音数学玩法共用的语音状态、历史面板和计时组件，减少专项玩法的重复实现。
5. 为 110 个短任务逐步增加经人工确认的本地发音资源、更多无文字观察任务和分年龄课程包；新增内容必须先通过结构校验。
